#pragma once
#include <iostream>
#include <vector>
#include "../Symbols.hpp"

namespace Preproces
{
std::string preProcessing(std::string);
std::string removeSpace(std::string);
bool validChar(char c);

} // namespace Preproces
